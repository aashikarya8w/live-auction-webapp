spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/clothes_auction}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:aashik123}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: true

  sql:
    init:
      mode: never

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:aashikrock1254@gmail.com}
    password: ${MAIL_PASSWORD:tduk awqp ihco jpbx}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:mysupersecretkeymysupersecretkeymysupersecretkey123}
  expiration: 86400000

google:
  client-id: ${GOOGLE_CLIENT_ID:your_google_client_id_here}

stripe:
  secret:
    key: ${STRIPE_SECRET_KEY:sk_test_your_stripe_secret_key_here}

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:your_cloud_name}
  api-key: ${CLOUDINARY_API_KEY:your_api_key}
  api-secret: ${CLOUDINARY_API_SECRET:your_api_secret}
